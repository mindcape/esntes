version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r backend/requirements.txt
run:
  command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
